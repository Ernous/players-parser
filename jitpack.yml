jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.2-open
  - sdk use java 17.0.2-open

env:
  - GRADLE_OPTS="-Xmx1024m"

script:
  - gradle build
  - gradle test
  - gradle publishToMavenLocal